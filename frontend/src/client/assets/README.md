# Client assets

Store approved, web-optimised Whitehouse assets here. Keep source design files
outside the application bundle and document any font licences before shipping.
`whitehouse-learning-mark.png` is the generated demo master artwork. Runtime
favicon and PWA variants are derived from it under `frontend/public`.
